{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReplayEvent",
  "description": "A single raw market event within a golden replay fixture stream (ndjson).",
  "type": "object",
  "properties": {
    "ts": { "type": "integer" }
  },
  "required": ["ts"]
}
