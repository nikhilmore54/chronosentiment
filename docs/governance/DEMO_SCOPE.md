# Replay-Certified Observability Demo — Scope

**Status:** COMPLETE — operational legitimacy threshold crossed (evidence generation, not new law)  
**Constitutional reference:** `.cursor/rules/chronosentiment-core.mdc`, `AUTHORITY_MAP.md` near-term execution posture step 1  
**Governance class:** operational legitimacy — **not** semantic convergence, **not** Lane 2 migration

---

## Purpose

Move the repository from:

```text
internally coherent
```

to:

```text
externally demonstrable
```

under **declared law already in force**. This demo generates evidence; it does not invent new constitutional surfaces.

The demo is **proof structure**, not marketing material. It should read as **forensic replay instrumentation** — not predictive financial software.

**Burden of proof:**

```text
bounded, attributable, replay-verifiable execution reality reconstruction
```

—not market superiority, prediction, or platform breadth.

---

## What the Demo Proves

| Protected asset | Demonstration |
|-----------------|---------------|
| **Chronology integrity** | Lawful persisted substrate loads; event ordering is deterministic |
| **Replay equivalence** | Same inputs → same reconstructed outcome (hash or comparator pass) |
| **Provenance** | Manifests, substrate hash, commit lineage (`resolve_git_commit_hash`) visible |
| **Causal reconstruction** | Event fold / replay reducer produces attributable state |
| **Execution divergence** | Intent vs realized interaction visible (where simulation exposes it) |
| **Bounded semantics** | No hidden ontology expansion; claims stay inside this document |

**Demo layer → user-visible narrative:**

| Layer | User sees |
|-------|-----------|
| Chronology capture | Ordered event stream from lawful substrate |
| Replay reconstruction | Deterministic fold replay (`reduce_replay_state` or harness path) |
| Divergence detection | Intent vs realized execution (bounded MVP surfaces) |
| Provenance | Hashes, manifests, commit lineage |
| Equivalence proof | Replay matches certified window or deterministic re-run |
| Observability narrative | “What happened and why” — causal trace, not prediction |

---

## What the Demo Explicitly Does *Not* Claim

| Non-claim | Reason |
|-----------|--------|
| Predictive alpha or trading edge | Outside bounded identity |
| Autonomous / AI trading | Identity inflation |
| Generalized market cognition or universal law | Unfalsifiable scope creep |
| Platform, SaaS, or dashboard empire | Presentation-driven expansion |
| Production-ready live capture authority | Lane 2 producers not ratified |
| Full `replay_response.schema.json` certification | Lane 3 adjacent — not Tier 1 gate |
| Resolution of all V-006 tranches | Lane 2 paused; tranche-scoped only |

```text
the demo proves bounded truth reconstruction
not market superiority
```

---

## Bounded Scenarios (initial)

Use **existing lawful chronology** under `core/chronology/historical/` — no new substrate emission without Lane 2 authorization.

| Priority | Scenario (catalog name) | Rationale |
|----------|-------------------------|-----------|
| **Primary** | `2026_multi_stage_cascade_transition` (1m or tick) | Multi-stage stress; already in catalog |
| **Alternate** | `2026_intraday_impulse_shock_0730_0800_utc` (1m or tick) | Impulse / fragmentation visibility |
| **Optional second panel** | `GETTING_STARTED.md` / `./chrono demo` path | Existing observer entry; proves tooling chain |

**Strategy identity replay (V-001 batch `003`)** may appear as a **secondary panel** only within the declared window in `REPLAY_WINDOW_DECLARATION_BATCH_003.md` — not conflated with chronology observatory demo unless explicitly labeled.

---

## Operational Flow (compose only — no new authority)

```text
1. Select bounded scenario (lawful chronology under core/chronology/historical/)
2. Verify substrate + manifest provenance (hash / catalog identity)
3. Materialize or replay through existing harness / tooling
4. Reconstruct state (reduce_replay_state or certified observatory path)
5. Show divergence / causal chain (where simulation exposes it)
6. Prove equivalence (deterministic re-run or certify_equivalence_v1.py)
7. Emit observable artifacts + short narrative (“what happened and why”)
```

Each step must use **allowed surfaces** only. If a step requires new chronology emission or serialization law → **stop** (Lane 2 escalation).

---

## Observable Outputs

Third-party runnable evidence (not slide deck):

| Output | Purpose |
|--------|---------|
| Run script or documented command sequence | Reproducibility |
| Substrate / manifest references | Chronology integrity + provenance |
| Replay state snapshot or trace summary | Causal reconstruction |
| Equivalence / certification report (JSON or stdout) | Replay equivalence |
| Commit hash + artifact hashes | Attribution |
| Short narrative (≤1 page) | Institutional legibility — causal, not predictive |

Outputs live under existing paths (`artifacts/`, `fixtures/demo/`, or documented stdout) — no new persistence law.

---

## Replay Guarantees (within scope)

| Guarantee | Mechanism |
|-----------|-----------|
| Deterministic replay | Same chronology bytes + same fold authority → same outcome |
| Substrate integrity | `chronology_hash` / manifest verification where applicable |
| Authority singularity | Operational replay via `reduce_replay_state`; no parallel folds |
| Provenance binding | Git commit + artifact hashes in observatory/cert paths |
| No silent byte mutation | No producer changes, no serialization changes during demo build |

**Pre-existing law relied upon (not re-declared):**

- V-001 strategy identity (`core/src/strategy_id.rs`) — if strategy panel used  
- V-006 serialization/dialect law — read-only; no migration  
- V-007 replay reducer authority  
- V-008 / V-009 / V-010 hygiene closures  

---

## Allowed Surfaces

Demo work may **compose** existing operational surfaces only:

| Surface | Role |
|---------|------|
| `core/chronology/historical/*` | Lawful persisted substrates |
| `core/src/harness.rs`, `ese.rs` | Deterministic simulation |
| `services/api/src/replay.rs` — `reduce_replay_state` | Operational event fold |
| `core/src/bin/trace_replay.rs` | Observatory artifact generation |
| `scripts/certify_equivalence_v1.py`, `emit_manifest_v1.py` | Certification tooling (STABLE TOOLING) |
| `services/ui` MVP replay slider | Optional thin client — no dashboard sprawl |
| `./chrono demo` / `GETTING_STARTED.md` | Existing entry path |

**New code permitted:** thin orchestration, demo script, minimal narrative copy, fixture outputs under `fixtures/` or `artifacts/` — **not** new authority domains.

---

## Non-Goals

The demo must **not** become:

- a dashboard sprawl project  
- a SaaS platform prototype  
- a generalized analytics suite  
- a “smart AI trader” presentation  
- a trigger for `capture_types.rs`, shared struct extraction, or Lane 2 migration  
- a reason to reopen Lane 1 or write Phase A/B/C governance stacks  

```text
an execution observability instrument under replay certification
```

—not a product maximizing feature breadth.

---

## Success Criteria

Tier 1 is **complete** when a third party can run a documented path and observe:

1. **Reproducibility** — same commands → same hashes/outcomes on a clean checkout  
2. **Attribution** — provenance fields are real (not placeholder literals)  
3. **Replay equivalence** — at least one certified or deterministic equivalence check passes  
4. **Causal narrative** — ordered events → reconstructed state → explainable divergence (where applicable)  
5. **Bounded claims** — demo README/scope matches what was shown; non-claims honored  
6. **No governance expansion** — no new constitutional docs unless inspection finds replay coupling (then escalate explicitly)

**Failure mode to avoid:** demo succeeds technically but reopens scope through presentation or feature creep.

---

## Escalation During Demo Build

| Discovery | Action |
|-----------|--------|
| Replay byte / substrate coupling | Stop; open **one** Lane 2 tranche scope — do not patch silently |
| Dual operational authority | Stop; Lane 1-class inspection — unlikely if scope honored |
| Hygiene-only issue | inspect → fix → verify → ledger note (compressed) |
| “Would be nicer with platform X” | **Reject** — out of scope |

---

## Relationship to Near-Term Posture

```text
1. this demo
2. Lane 3 operational hardening
3. reassess one bounded V-006 tranche (likely manifest ms)
4. Phase 4 refinement as needed
```

This document bounds step 1 only. Completing the demo does **not** authorize step 3 automatically.

---

## References

- `AUTHORITY_MAP.md` — near-term execution posture, protected assets, governance compression  
- `GETTING_STARTED.md` — existing observer entry  
- `docs/governance/REPLAY_WINDOW_DECLARATION_BATCH_003.md` — optional strategy panel window  
- `docs/governance/V006_PHASE_C_CLASSIFICATION.md` — Lane 2 paused posture  
- `docs/governance/V007_TYPE_AUTHORITY_DECISION.md` — replay reducer law  
