[package]
name = "chronosentiment_core"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
# core dependencies only
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
