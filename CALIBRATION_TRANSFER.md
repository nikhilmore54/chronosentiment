# CALIBRATION_TRANSFER.md
# ChronoSentiment — Calibration Phase Transfer Summary
# Last updated: 2026-05-27
# Purpose: Portable context artifact for IDE/model transfer.
#          Reflects calibrated state per `.cursor/rules/chronosentiment-core.mdc`,
#          `AUTHORITY_MAP.md`, and completed governance lanes.
#          This document is OBSERVATIONAL — it describes state; it does not define law.
#          Canonical authority registry remains: AUTHORITY_MAP.md

---

## Core Identity

ChronoSentiment is no longer optimizing for conceptual breadth, platformization, or generalized intelligence.

The repository converged on:

```text
stable attributable observation under controlled interpretation
```

The conserved quantity is:

```text
replay meaning
```

Every implementation decision in Phase 3A+ was evaluated against:

```text
does this reduce entropy around attributable replay truth
without widening semantic authority?
```

---

## Architectural Maturity State

The repo behaves as:

```text
a calibrated replay-governed operational instrument
```

It is NOT:

- an evolving research ontology
- a generalized replay platform
- a governance framework
- a semantic ecosystem

The operational grammar encoded in `AUTHORITY_MAP.md` is:

```text
observe → classify → freeze evidence → surface escalation
```

NOT:

```text
normalize → converge → institutionalize
```

---

## Calibration-Preservation Layer

`AUTHORITY_MAP.md` acts as a constitutional compression layer for calibration preservation.

### Encoded principles

| Principle | Meaning |
|-----------|---------|
| replay meaning is conserved | identity-bearing replay semantics must remain stable |
| observational ≠ authoritative | tooling/evidence may verify law but not define law |
| legitimacy ≠ capability | capability growth must not widen authority |
| ambient semantic accumulation forbidden | drift must appear as explicit escalation |
| calibration discipline is permanent | not temporary stabilization |

### Four-question executable filter

All new work must pass this filter before implementation:

| Question | Purpose |
|----------|---------|
| Who may define meaning? | authority control |
| What may mutate? | calibration preservation |
| What must remain replay-stable? | determinism protection |
| What escalation becomes mandatory? | drift visibility |

---

## Lane Status (Current)

| Lane | Status | Meaning |
|------|--------|---------|
| Lane 1 | STABILIZED | authority convergence complete |
| Lane 2 | PREREQUISITES_CLARIFIED — NOT OPEN | no migration authorized |
| Lane 3 | PAUSED (slices 1–3 shipped) | operational reproducibility baseline complete |
| Lane 4 | COMPLETE | research lineage bounded |

Current posture:

```text
pause under calibration
```

This is intentional and healthy.

---

## Lane 1 — Authority Convergence (Complete)

Resolved:

- V-001 strategy identity law
- V-007 replay reducer authority
- V-008 test asset path authority
- V-009 config hash authority
- V-010 observatory provenance authority

Key result:

```text
authority singularity stabilized
```

No dual operational folds remain.

---

## Lane 2 — Replay Substrate Law

**Lane 2 is NOT open.**

Only prerequisite clarification work has been completed. No migration, producer alignment, schema convergence, or `capture_types.rs` work is authorized.

### Completed prerequisite units

**1. Manifest ms impact inventory** — commit `9d2c493c`

Artifacts: `V006_MANIFEST_MS_IMPACT_INVENTORY_SCOPE.md`, `lane2_manifest_ms_impact_inventory.py`, frozen fixture, CI reference check.

Findings: 60 manifests scanned — 13 seconds-bound; 12 live-capture forward-law violations; 1 missing-bounds anomaly.

Core asymmetry:

```text
inventory maps exposure
inventory does not design migration
```

**2. Manifest ms assumption probe** — commit `cc5c28fd`

Artifacts: `V006_MANIFEST_MS_ASSUMPTION_PROBE_SCOPE.md`, assumption probe script, frozen fixture, CI reference check.

Findings: Tier1 demo implicitly assumes ms semantics; `rebuild_catalog.py` contains heuristic unit handling; producer emits seconds bounds via `as_secs()`; no replay-law widening occurred.

Core asymmetry:

```text
probe exposes assumptions
probe does not correct semantics
```

### Lane 2 reopening condition

DO NOT reopen because prerequisites are complete, migration is understood, tranche is easy, or work is available.

Reopen ONLY when:

```text
bounded operational ambiguity
becomes more costly than bounded migration
```

Most likely first tranche if reopened: manifest ms correction — ONLY via explicit tranche scope doc, four-question PASS, `AUTHORITY_MAP.md` update, and bounded replay-impact declaration.

---

## Lane 3 — Certifiability Infrastructure

**Lane 3 is PAUSED after slices 1–3. There is no "finish Lane 3" roadmap. Each slice must independently earn admissibility.**

**Slice 1 — Deterministic replay rerun guard**

Purpose: same execution path → same artifacts.

**Slice 2 — Reproducibility envelope freeze**

Purpose: environment-bounded reproducibility.

Key asymmetry:

```text
environment variance is observational
replay instability is constitutional
```

Included: git commit, rust version, cargo lock hash, OS fingerprint, deterministic replay hash.
Excluded: timestamps, hostname, CPU IDs, durations, absolute paths.

**Slice 3 — Artifact integrity attestation**

Purpose: tamper-visible attestation continuity.

Critical invariant:

```text
integrity may attest to replay meaning
integrity may not define replay meaning
```

Empirical gate outcome: `PASS_PASSIVE_RECOMPUTATION_SUFFICIENT`

Key result: verification scope widened; replay law did not widen.

Admissibility blocks A–G recorded in `AUTHORITY_MAP.md`. Tamper cases T1–T3 detected.

### Lane 3 reopening condition

Open ONLY if a concrete operational blind spot creates ambiguity around attributable replay truth.

Strong future candidates (only if operationally necessary): completeness verifier, binary replay failure classifier, retention consistency checker.

Reject: replay registries, orchestration, provenance platforms, dashboards, generalized certification infrastructure.

---

## Lane 4 — Authority Boundary Containment (Complete)

Commit: `6e518290`

Artifacts: `RESEARCH_LINEAGE.md`, containment scope note, audit script, frozen fixture, CI job.

Audit findings: 93 surfaces scanned — 4 detached archives; 7 semantic duplicates; 58 downstream consumers; 24 observational; 0 authority grants; 0 map mismatches.

Core asymmetry preserved:

```text
research preserved
authority bounded
```

---

## Operational Philosophy

The repo intentionally resists:

- platform gravity
- orchestration pressure
- governance maximalism
- ontology expansion
- semantic accumulation
- lane fusion

Progress is measured as:

```text
reduced entropy around attributable replay truth
```

NOT feature count, abstraction count, architectural breadth, or conceptual sophistication.

---

## Current Recommended Posture

```text
pause under calibration
```

The repository is stable enough to survive stillness without identity drift. That is a major convergence signal.

---

## If Future Work Resumes

**Safest direction:** calibration audits or containment reinforcement.

**Most likely future Lane 2 tranche:** manifest ms correction — ONLY when operational ambiguity becomes materially costly.

**Most important rule:**

```text
verification scaling must not become authority scaling
```

And:

```text
attestation continuity may strengthen
semantic authority may not widen
```

---

## Final State Summary

ChronoSentiment currently behaves as:

```text
a calibrated replay-governed infrastructure instrument
```

with:

- bounded interpretation
- deterministic recoverability
- explicit authority topology
- replay equivalence discipline
- operationally visible escalation
- stable pause behavior
- preserved calibration under external exposure

The boundary held.

---

## Next-Step Posture (Ranked)

The following is a ranked orientation — not a roadmap. Default remains `pause under calibration`.

| Tier | Direction | Gate |
|------|-----------|------|
| **A** | Branch hygiene — isolate existing unstaged work (V-001 evidence, V-006 fixtures, API routing, UI) into clean semantic commits; run CI | No new lanes required |
| **B** | Lane 4 minor follow-up — audit baseline expansion, `docs/research_archive/governance_audit_2026-05-25.md`, script relocation scope note only | Still observational; no convergence |
| **C** | Lane 2 prerequisite only — producer drift measurement matrix or tranche dependency graph | Stop if it feels like migration prep |
| **D** | Lane 2 tranche unlock — one tranche scope doc + four-question PASS + `AUTHORITY_MAP.md` update | Constitutional decision; open only when operationally necessary |
| **E** | Lane 3 slice 4 — artifact completeness verifier or binary replay failure classifier | Per-slice admissibility gate; litmus: does success make the repo smaller and calmer? |

### What to reject now

| Direction | Reason |
|-----------|--------|
| Unified replay platform | lane fusion |
| Finish Lane 3 for symmetry | momentum, not need |
| Producer convergence / `capture_types.rs` | constitutional without tranche |
| Provenance UI / dashboard | interpretive gravity |
| Back-to-back Lane 2 inventories | convergence atmosphere |

### Practical 30-day sequence (if actively developing)

```text
Week 1  — branch hygiene: commit/push isolated tranches of existing unstaged work + green CI
Week 2  — pause OR Lane 4 minor containment (audit baseline only)
Week 3  — reassess: is any Lane 2 tranche operationally necessary?
Week 4  — if yes → scope doc only; if no → stay paused
```

### Single honest recommendation

**Next development step:** finish and isolate already-in-progress branch work (V-001 evidence, API routing, fixtures) into clean commits — then pause.

**Next governance step (only if needed):** authorize one Lane 2 tranche via scope doc — not because the repo lacks clarity, but because replay-law change becomes operationally necessary.

**Next wrong step:** opening Lane 3 slice 4 or Lane 2 migration because momentum exists after a strong calibration run.

---

## Tier A — Branch Hygiene Implementation Sequence

This is not feature development. It is:

```text
semantic separation + replay-safe stabilization
```

### Unit A1 — V-001 Governance Evidence Isolation

Scope: governance docs, calibration evidence, `fixtures/strategy_identity/` only.

Goal: separate governance evidence from runtime behavior.

Verification: `cargo check` + `./scripts/verify_strategy_identity.sh` (if present).

Commit boundary:
```text
docs(governance): ratify V-001 calibration evidence + strategy identity fixtures
```

### Unit A2 — V-006 Fixture / Byte-Law Isolation

Scope: byte fixtures, chronology verification scripts, replay certification artifacts. No routing changes.

Goal: preserve byte-law doctrine without accidentally authorizing migration work.

Verification: `cargo test chronology` (or equivalent deterministic fixture suite) + `determinism-check.yml` locally if possible.

Commit boundary:
```text
test(replay): add chronology byte fixtures + replay verification artifacts
```

### Unit A3 — API / Core Routing Isolation

Highest-risk Tier A unit — can silently drift into tranche behavior.

Allowed: [`strategy_id.rs`](core/src/strategy_id.rs), edge routing cleanup, replay-safe refactors, deterministic path corrections.

Forbidden: producer convergence, schema mutation, replay-law alteration, capture-type migration logic. Especially avoid [`capture_types.rs`](cs-ingest/src/main.rs) until explicit tranche authorization.

Required question before commit:
```text
Does this alter replay meaning?
```
If the answer is "maybe" → stop.

Verification: `cargo check` + `cargo test replay` + `cargo test determinism`.

Commit boundary:
```text
refactor(core): isolate replay-safe routing normalization
```

### Unit A4 — UI Isolation

Treat UI strictly as projection hygiene, not semantic interpretation.

Allowed: layout cleanup, observer ergonomics, replay visualization polish, state projection consistency.

Forbidden: provenance ontology, inference overlays, semantic scoring layers, interpretive dashboards. UI must remain observer-only per service-boundary law.

Commit boundary:
```text
ui: stabilize observer projections + replay ergonomics
```

---

## Phase 2 — Verification Pass (After All Units Isolated)

```bash
cargo check
cargo test
gh workflow run determinism-check.yml
```

### Required certification questions before merge/push

| Question | Required Answer |
|----------|-----------------|
| Did replay meaning change? | NO |
| Did causal ordering change? | NO |
| Did authority boundaries widen? | NO |
| Did UI acquire semantic authority? | NO |
| Did implementation imply tranche authorization? | NO |

If any answer becomes uncertain → pause.

---

## Phase 3 — Post-Hygiene Pause

After Tier A completion: do NOT immediately open Lane 4 follow-up, do NOT start tranche prep, do NOT continue because momentum exists.

Instead:

```text
green CI
→ replay certification
→ observational soak
→ reassess necessity
```

That sequence is aligned with the repository's stabilized identity:

```text
bounded replay instrument
```

not:

```text
continuously expanding platform
```

---

## Cross-References

| Document | Role |
|----------|------|
| [`AUTHORITY_MAP.md`](AUTHORITY_MAP.md) | Constitutional compression layer; canonical authority registry — **primary source of truth** |
| [`GOVERNANCE.md`](GOVERNANCE.md) | Freeze policy; operational scope constraint |
| [`docs/RESEARCH_LINEAGE.md`](docs/RESEARCH_LINEAGE.md) | Research history preservation; authority boundary |
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | Component layer documentation |
| [`DISCREPANCY_REPORT.md`](DISCREPANCY_REPORT.md) | Discrepancy tracking |