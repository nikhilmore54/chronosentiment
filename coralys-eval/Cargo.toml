[package]
name = "coralys-eval"
version = "0.1.0"
edition = "2024"
description = "Coralys Evaluation Framework — benchmark-independent evaluation interfaces, adapter registry, and evaluation pipeline"

[dependencies]
coralys-core = { path = "../coralys-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
# No external test deps needed; framework tests use only in-crate types